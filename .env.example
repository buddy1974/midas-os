DATABASE_URL=
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000
ANTHROPIC_API_KEY=
RESEND_API_KEY=

# Shared secret for website → OS intake API calls
# Generate with: openssl rand -hex 32
WEBSITE_API_KEY=

# Separate secret for CMS write endpoint (website CMS tab saves config)
# Generate with: openssl rand -hex 32
CMS_WRITE_TOKEN=
